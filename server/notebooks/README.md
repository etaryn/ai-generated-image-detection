Scratch space for data exploration (class balance checks, sample image grids,
augmentation previews, etc.). Not part of the required deliverables — keep
experiment-only work here rather than in the core `data/`, `model/`, or `eval/`
packages.
