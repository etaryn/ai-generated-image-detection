"""Frozen feature extractors: DINOv2 (structure/texture), CLIP (semantics),
FFT statistics (pixel-level noise). See features/pipeline.py for the stack that
concatenates them into one vector per image."""
